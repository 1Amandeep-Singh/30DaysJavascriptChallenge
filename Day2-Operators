//Activity 1: Arithmetic Operations

var firstelement = 10;
console.log("First Element: " + firstelement);
var secondelement = 5;
console.log("Second Element: " + secondelement);

//Task 1: Write a program to add numbers and log the result to the console
var sum = firstelement + secondelement;
console.log("The sum of two number is " + sum);

//Task 2: Write a program to subtract numbers and log the result to the console
var sub = firstelement - secondelement;
console.log("The subtract of two number is " + sub);

//Task 3: Write a program to multiply numbers and log the result to the console
var mul = firstelement * secondelement;
console.log("The multiply of two number is " + mul);

//Task 4: Write a program to divide numbers and log the result to the console
var div = firstelement / secondelement;
console.log("The divison of two number is " + div);

//Task 5: Write a program to find the remainder when one number is divided by another and log the result to the console
var rem = firstelement % secondelement;
console.log("The remainder when one number is divided by another is " + rem);
//Activity 1 end

//Activity 2: Assignment Operators

//Task 6: Use the += operator to add a number to a variable and log the result to the console
firstelement += secondelement;
console.log("The value of firstelement using (+=) operator is " + firstelement);

//Task 7: Use the += operator to add a number to a variable and log the result to the console
firstelement -= secondelement;
console.log("The value of firstelement using (-=) operator is " + firstelement);

//Activity 2 end

//Activity 3: Comparison Operators

//Task 8: Write a program to compare two numbers using > and < and log the result to the console
var greaterThan = firstelement > secondelement;
console.log(
  "The value on using (>) greater than operator return bool value such that: " +
    greaterThan
);

var lessThan = firstelement < secondelement;
console.log(
  "The value on using (>) less than operator return bool value such that: " +
    lessThan
);

//Task 9: Write a program to compare two numbers using >= and <= and log the result to the console
var greaterThanEqualto = firstelement >= secondelement;
console.log(
  "The value on using (>=) operator return bool value such that: " +
    greaterThanEqualto
);

var LessThanEqualto = firstelement <= secondelement;
console.log(
  "The value on using (<=) operator return bool value such that: " +
    LessThanEqualto
);

//Task 9: Write a program to compare two numbers using == and === and log the result to the console
var doubleEqualTo = firstelement == secondelement;
console.log(
  "The double equal(==) operator test only value not its type and return bool value: " +
    doubleEqualTo
);

var tripleEqualTo = firstelement == secondelement;
console.log(
  "The triple equal(===) operator test value and its type and return bool value: " +
    tripleEqualTo
);

//Activity 3 end

//Activity 4: Logical Operators

//Task 11: Write a program that uses the && operator to combine two conditions and log the result to the console
var andOperator = firstelement && secondelement;
console.log("The value on using (&&) AND operator : " + andOperator);

//Task 12: Write a program that uses the || operator to combine two conditions and log the result to the consoled
var orOperator = firstelement || secondelement;
console.log("The value on using (||) OR operator : " + orOperator);

//Task 13: Write a program that uses the ! operator to combine two conditions and log the result to the consoled
var notOperator = !firstelement;
console.log("The value on using (!) NOT operator : " + notOperator);

//Activity 4 end

//Activity 5: Ternary Operators

//Task 14: Write a program that uses the ternary operator to check if a number is positive or negative and log the result to the consolefunction isNumberPositive(firstelement){
var bresult = firstelement >= 0 ? "Positive Number" : "Negative number";
console.log(
  "The value on using (?) Ternary Operator return first value i.e. true if satisfy conditionalExpression otherwise second value or false: " +
    bresult
);

//Activity 5 end
